import React, {Component} from 'react';

export default class NotFound extends Component{
  render(){
    return (
      <section className='flex'>
        <p>It looks like you're trying to reach a page that doesn't exist. Nice try!</p>
      </section>
    );
  }
}